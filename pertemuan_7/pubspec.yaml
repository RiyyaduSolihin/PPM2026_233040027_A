name: pertemuan_5
description: "Mini App Catatan Mahasiswa — REST API & CRUD Jaringan dengan HTTP."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8


  http: ^1.2.0      # HTTP client resmi Dart (ganti sqflite + path)

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true